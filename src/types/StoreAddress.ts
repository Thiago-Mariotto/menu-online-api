export type TInputStoreAddress = {
  complement: string;
  number: string;
  addressId: string;
}

export type TStoreAddress = {
  storeAddressId: string;
  complement: string;
  number: string;
  addressId: string;
}